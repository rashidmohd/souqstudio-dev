import { vitestBase } from '@souqstudio/config/vitest.base'

// Pure functions over plain data — no DOM, no setup file, no aliases. The base
// config is the whole configuration.
export default vitestBase
