# Brand assets

Committed and in use. Usage rules, variant selection and the open casing question:
`.claude/skills/souqstudio-design/references/brand-assets.md`

Colours are `#143CD2` (blue) and `#323232` (charcoal), both matching the token file.
The source files carried `#153CD0`; that has been corrected here.

Rasters are generated from the SVGs. If the mark changes, replace the SVGs and
regenerate — never hand-edit a PNG.

One outstanding action: `email/logo-dark.png` must be uploaded to R2 at
`https://assets.souqstudio.com/email/logo-dark.png`. The email header references that
absolute URL and will render a broken image until it resolves.
